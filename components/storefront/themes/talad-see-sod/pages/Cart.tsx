'use client';

/**
 * talad-see-sod — Cart page (shopping-cart-04)
 */
import { makeCartAdapter } from '@/components/storefront/themes/_shared/cart-adapter';
export const CartPage = makeCartAdapter('04');
export default CartPage;
