'use client';

/**
 * saluki-yoga — Catalog page (product-category-03)
 */
import { makeCatalogAdapter } from '@/components/storefront/themes/_shared/catalog-adapter';
export const CatalogPage = makeCatalogAdapter('03');
export default CatalogPage;
