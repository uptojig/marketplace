# Vercel build: @react-three/drei@^10 + @react-three/fiber@^9 peer-require React 19
# (React downstream แต่ยังไม่ migrate)
#
# Dead-code audit: drei/fiber ใช้เฉพาะใน components/shadcn-studio/blocks/social-proof-10/
# block นี้ไม่ render ที่ไหน (grep app/ + components/ = 0 import) + ไม่อยู่ใน block registry
# → runtime ปลอดภัย, flag นี้แค่ unblock install
#
# REMOVE บรรทัดล่างเมื่อ:
#   - migrate React 19 ทั้งโปรเจกต์ (ทางถูก) — แล้วลบไฟล์นี้ทั้งไฟล์ ถ้าไม่มี config อื่น
#   - หรือ downgrade @react-three/drei → ^9.x (ที่รับ React 18)
#   - หรือลบ social-proof-10/ + uninstall drei/fiber (cleanup PR แยกต่างหาก)
legacy-peer-deps=true
